Ты — опытный full-stack разработчик. Твоя задача — реализовать все **невыполненные** задачи из плана `1plans/mass_reaction.md` (задачи помечены `- [ ]`). После выполнения каждой задачи:
- внеси все необходимые изменения в код,
- запусти форматирование/линтеры, если это принято в проекте,
- **сделай отдельный git-коммит только с изменениями по этой задаче** с коротким комментарием на русском (например: `"добавить таблицу reaction_jobs"`, `"реализовать CRUD эндпоинты кампаний"`, `"сделать страницу автореакций"`),
- **сделай `git push`** текущей ветки на origin,
- пометь задачу как выполненную `- [x]` прямо в файле плана `./1plans/mass_reaction.md`

## Контекст проекта

**Backend:** FastAPI (Python), запущен на `http://localhost:8000`. Все Telegram-операции идут через него.

**Frontend:** Next.js 14 (App Router), TypeScript, папка `FrontEnd/`. Страницы Dashboard находятся в `FrontEnd/app/(dashboard)/`.

**Ключевые API-эндпоинты:**
- `GET /sessions` → список сессий (поле `sessions`, каждая содержит `session_id`, `phone`, `is_authorized`)
- `GET /sessions/{session_id}/account/me` → информация об аккаунте (`first_name`, `last_name`, `username`, `phone`, `photo`)
- `POST /sessions/{session_id}/auth/login` body: `{ "phone": "+7..." }` → отправить код (ответ: `{ success, message }`)
- `POST /sessions/{session_id}/auth/verify` body: `{ "phone": "+7...", "code": "12345" }` → подтвердить код (ответ: `{ success, password_required, message }`)
- `POST /sessions/{session_id}/auth/password` body: `{ "password": "..." }` → ввести 2FA пароль (ответ: `{ success, message }`)

Для модуля «Автореакции» ты также добавляешь новые эндпоинты (см. план `mass_reaction.md`), например:
- `GET /users/{user_id}/reaction-jobs` → список кампаний пользователя;
- `POST /users/{user_id}/reaction-jobs` → создать кампанию;
- `PATCH /users/{user_id}/reaction-jobs/{job_id}` → обновить/переключить тумблер;
- `DELETE /users/{user_id}/reaction-jobs/{job_id}` → удалить кампанию.

## Требования к реализации

1. **Следуй плану из `1plans/mass_reaction.md`:** выполняй задачи по порядку: база данных → backend (схемы + CRUD) → backend (воркер) → frontend.

2. **База данных (Supabase):**
   - Создай миграцию SQL для таблицы `reaction_jobs` с полями из плана;
   - добавь RLS-политики и индексы;
   - не коммить реальные ключи/URL от Supabase.

3. **Backend-задачи:**
   - Добавляй Pydantic-схемы в `app/models.py` и новые роуты в `app/main.py`;
   - расширяй `app/telegram_client.py` аккуратно, не ломая существующие методы авторизации и работы с сессиями;
   - учитывай лимиты и обработку ошибок Telethon (FloodWait, недоступные чаты, отсутствие прав на реакции);
   - для фонового воркера используй `asyncio` задачи или APScheduler.

4. **Frontend-страница «Автореакции»:**
   - вынеси константу `API_BASE = "http://localhost:8000"` в начало файлов, где делаешь запросы;
   - сделай UX, аналогичный другим страницам Dashboard: состояния loading/empty/error, аккуратная верстка;
   - название в навигации и интерфейсе — **«Автореакции»** (не «Jobs» и не «Tasks»);
   - у каждой кампании в списке: название слева, справа — иконка карандаша (редактировать), иконка корзины (удалить), тумблер вкл/выкл;
   - модальное окно создания/редактирования открывается как по кнопке «+ Создать кампанию», так и по клику на карандаш (предзаполняется).

## Важно

- Не ломай существующие страницы (`home`, `page.tsx` — форма логина, `accounts`, `channels-parser`).
- TypeScript строгий: типизируй все ответы API через `interface`.
- После каждого выполненного пункта плана — помечай его `- [x]` в `1plans/mass_reaction.md`.
- Если задача уже выполнена (помечена `- [x]`) — пропускай её.
