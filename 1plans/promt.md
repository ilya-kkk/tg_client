Ты — опытный full-stack разработчик. Твоя задача — реализовать все **невыполненные** задачи из плана `1plans/plan_parse_channels.md` (задачи помечены `- [ ]`). После выполнения каждой задачи:
- внеси все необходимые изменения в код,
- запусти форматирование/линтеры, если это принято в проекте,
- **сделай отдельный git-коммит только с изменениями по этой задаче** с коротким комментарием на русском (например: `\"реализовать поиск каналов\"`, `\"добавить эндпоинт парсера каналов\"`, `\"сделать страницу парсера каналов\"`),
- **сделай `git push`** текущей ветки на origin,
- пометь задачу как выполненную `- [x]` прямо в файле плана `./1plans/plan_parse_channels.md`

## Контекст проекта

**Backend:** FastAPI (Python), запущен на `http://localhost:8000`. Все Telegram-операции идут через него.

**Frontend:** Next.js 14 (App Router), TypeScript, папка `FrontEnd/`. Страницы Dashboard находятся в `FrontEnd/app/(dashboard)/`.

**Ключевые API-эндпоинты:**
- `GET /sessions` → список сессий (поле `sessions`, каждая содержит `session_id`, `phone`, `is_authorized`)
- `GET /sessions/{session_id}/account/me` → информация об аккаунте (`first_name`, `last_name`, `username`, `phone`, `photo`)
- `POST /sessions/{session_id}/auth/login` body: `{ "phone": "+7..." }` → отправить код (ответ: `{ success, message }`)
- `POST /sessions/{session_id}/auth/verify` body: `{ "phone": "+7...", "code": "12345" }` → подтвердить код (ответ: `{ success, password_required, message }`)
- `POST /sessions/{session_id}/auth/password` body: `{ "password": "..." }` → ввести 2FA пароль (ответ: `{ success, message }`)

Для модуля «Парсер каналов» ты также будешь добавлять новые эндпоинты (см. план `plan_parse_channels.md`), например:
- `POST /sessions/{session_id}/channels/search` → поиск каналов по ключевым словам;
- опциональные эндпоинты для сохранения и выборки результатов парсинга.

## Требования к реализации

1. **Следуй плану из `1plans/plan_parse_channels.md`:** выполняй задачи по порядку или в логически удобной последовательности (backend → сохранение → frontend), но не пропускай пункты.

2. **Backend-задачи:**
   - Добавляй Pydantic-схемы и новые роуты в соответствии с планом;
   - расширяй `app/telegram_client.py` аккуратно, не ломая существующие методы авторизации и работы с сессиями;
   - учитывай лимиты и обработку ошибок Telethon (FloodWait, приватные/недоступные каналы).

3. **Хранение результатов (если делаешь этот блок):**
   - продумай структуру таблиц так, чтобы можно было переиспользовать базу каналов в будущем (поиск, фильтры, обновление статуса);
   - не коммить реальные ключи/URL от Supabase или другой БД.

4. **Frontend-страница парсера:**
   - вынеси константу `API_BASE = "http://localhost:8000"` в начало файлов, где делаешь запросы;
   - сделай UX, аналогичный другим страницам Dashboard (состояния loading/empty/error, аккуратная верстка).

## Важно

- Не ломай существующие страницы (`home`, `page.tsx` — форма логина).
- TypeScript строгий: типизируй все ответы API через `interface`.
- После каждого выполненного пункта плана — помечай его `- [x]` в `1plans/plan_parse_channels.md`.
- Если задача уже выполнена (помечена `- [x]`) — пропускай её.
