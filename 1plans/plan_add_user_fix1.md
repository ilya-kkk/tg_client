## Страница «Аккаунты» — добавление и отображение Telegram-аккаунтов

### 1. Переименование роута и ссылки в сайдбаре
- [x] **Переименовать директорию** `FrontEnd/app/(dashboard)/add-account/` → `FrontEnd/app/(dashboard)/accounts/`
- [x] **Обновить сайдбар** `FrontEnd/app/(dashboard)/layout.tsx`
  - Изменить текст ссылки с «Добавить аккаунт» на «Аккаунты»
  - Изменить `href` с `/add-account` на `/accounts`

### 2. Страница «Аккаунты» — список аккаунтов
- [x] **Создать компонент страницы** `FrontEnd/app/(dashboard)/accounts/page.tsx` (клиентский, `"use client"`)
  - При загрузке: `GET http://localhost:8000/sessions` → показать список авторизованных сессий (`is_authorized === true`)
  - Для каждой сессии дополнительно запросить `GET http://localhost:8000/sessions/{session_id}/account/me` → получить `first_name`, `last_name`, `username`, `phone`, `photo` (base64/url)
  - Отображать аккаунты в виде карточек:
    - Слева — аватарка (круглая; если нет фото — заглушка с инициалами)
    - Имя и фамилия (крупный шрифт)
    - Телефон (мелкий текст)
    - Username (`@username`, если есть)
  - Сверху над карточками — кнопка «Добавить аккаунт»
  - Состояния: загрузка (скелетон или спиннер), пустой список («Нет аккаунтов»), ошибка
- [x] **Константа API_BASE** = `http://localhost:8000` вынесена в начало файла (не хардкодить в каждом fetch)

### 3. Модальное окно добавления аккаунта
- [x] **Создать компонент** `FrontEnd/app/(dashboard)/accounts/AddAccountModal.tsx`
  - Три шага в одном модальном окне:

#### Шаг 1 — Ввод номера телефона
  - [x] Поле ввода номера (placeholder `+79991234567`)
  - [x] Валидация формата в реальном времени (regex `^\+\d{7,15}$`); пока невалидно — кнопка «Отправить код» неактивна
  - [x] Кнопка «Отправить код» справа от поля (активируется после валидации)
  - [x] При клике: сгенерировать `session_id = crypto.randomUUID()`, запрос `POST /sessions/{session_id}/auth/login` с `{ phone }` → при успехе перейти на шаг 2

#### Шаг 2 — Ввод кода подтверждения
  - [x] 5 отдельных однозначных input-полей для цифр (автопереход фокуса при вводе цифры)
  - [x] Поддержка вставки всего кода сразу (`onPaste` — заполнить все 5 полей)
  - [x] При заполнении всех 5 цифр — автоматически отправить `POST /sessions/{session_id}/auth/verify` с `{ phone, code }`
  - [x] Если ответ `success=true` и `password_required=false` → закрыть модал, обновить список аккаунтов
  - [x] Если ответ `password_required=true` → перейти на шаг 3

#### Шаг 3 — Ввод 2FA пароля (только если нужен)
  - [x] Поле для ввода пароля (по умолчанию `type="password"`, символы скрыты звёздочками)
  - [x] Иконка глазика справа внутри поля: пока зажата левая кнопка мыши — показывать пароль (`type="text"`), отпустил — скрыть
  - [x] Кнопка «Войти» по центру под полем
  - [x] При клике: `POST /sessions/{session_id}/auth/password` с `{ password }` → при `success=true` закрыть модал, обновить список

#### Общее поведение модала
  - [x] Backdrop + блок по центру экрана; закрывается крестиком или кликом вне окна
  - [x] Показывать inline-ошибку под активным шагом при неуспешном ответе API
  - [x] Кнопка/ссылка «Назад» на шаге 2 и 3 для возврата к предыдущему шагу
  - [x] Состояние загрузки на кнопках (disabled + спиннер) пока идёт запрос

### 4. Обновление данных после добавления
- [x] После закрытия модала при успехе — вызвать функцию перезагрузки списка аккаунтов (перезапросить `GET /sessions` + `account/me` для новой сессии)

### 5. Стили
- [x] Карточки аккаунтов — flexbox, gap, border-radius, box-shadow
- [x] Аватарка — круглая, 56×56px; инициалы по центру на цветном фоне (цвет берётся из хэша имени)
- [x] Модальное окно — белый блок, max-width 420px, padding, rounded corners, backdrop с blur
- [x] 5 полей кода — квадратные, крупный шрифт, border highlight при фокусе
- [x] Все стили через CSS Modules (`accounts.module.css`, `modal.module.css`) или Tailwind (если уже используется в проекте)
